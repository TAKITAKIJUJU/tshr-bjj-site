---
description: サンプルの自動実行ワークフロー
---

// turbo-all

このワークフローは `// turbo-all` の指定があるため、記載されているすべてのターミナルコマンドが許可なしで自動実行されます。
ご自身の自動化したい作業に合わせて、このファイル内のコマンドを自由に書き換えてご活用ください。

1. Node.js と npm のバージョンを確認する
```powershell
node -v
npm -v
```

2. 現在のディレクトリの日付等の情報を確認する
```powershell
Get-Date
```
